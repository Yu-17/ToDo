<h1>編集する</h1>
<%= form_with model:@list, url:"/lists/#{@list.id}", local:true do |f| %>
    <h4>title</h4>
    <%= f.text_field :title %>
    <h4>text</h4>
    <%= f.text_area :body %><br>
    <h4>投稿日時</h4>
    <%= f.text_area :created_at %><br>
    <%= f.submit '保存' %>
<% end %>